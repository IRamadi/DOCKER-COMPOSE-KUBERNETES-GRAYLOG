apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: opensearch-pvc
  namespace: graylog
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: opensearch
  namespace: graylog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: opensearch
  template:
    metadata:
      labels:
        app: opensearch
    spec:
      containers:
      - name: opensearch
        image: opensearchproject/opensearch:2.11.0
        ports:
        - containerPort: 9200
        - containerPort: 9600
        env:
        - name: cluster.name
          valueFrom:
            configMapKeyRef:
              name: graylog-config
              key: OPENSEARCH_CLUSTER_NAME
        - name: node.name
          valueFrom:
            configMapKeyRef:
              name: graylog-config
              key: OPENSEARCH_NODE_NAME
        - name: discovery.type
          valueFrom:
            configMapKeyRef:
              name: graylog-config
              key: OPENSEARCH_DISCOVERY_TYPE
        - name: bootstrap.memory_lock
          value: "true"
        - name: OPENSEARCH_JAVA_OPTS
          valueFrom:
            configMapKeyRef:
              name: graylog-config
              key: OPENSEARCH_JAVA_OPTS
        - name: DISABLE_INSTALL_DEMO_CONFIG
          valueFrom:
            configMapKeyRef:
              name: graylog-config
              key: DISABLE_INSTALL_DEMO_CONFIG
        - name: DISABLE_SECURITY_PLUGIN
          valueFrom:
            configMapKeyRef:
              name: graylog-config
              key: DISABLE_SECURITY_PLUGIN
        resources:
          limits:
            memory: 2Gi
          requests:
            memory: 1Gi
        volumeMounts:
        - name: opensearch-storage
          mountPath: /usr/share/opensearch/data
        securityContext:
          capabilities:
            add:
              - IPC_LOCK
      volumes:
      - name: opensearch-storage
        persistentVolumeClaim:
          claimName: opensearch-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: opensearch
  namespace: graylog
spec:
  selector:
    app: opensearch
  ports:
  - name: http
    port: 9200
    targetPort: 9200
  - name: transport
    port: 9600
    targetPort: 9600
  type: ClusterIP

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: opensearch-dashboards
  namespace: graylog
spec:
  replicas: 1
  selector:
    matchLabels:
      app: opensearch-dashboards
  template:
    metadata:
      labels:
        app: opensearch-dashboards
    spec:
      containers:
      - name: opensearch-dashboards
        image: opensearchproject/opensearch-dashboards:2.11.0
        ports:
        - containerPort: 5601
        env:
        - name: OPENSEARCH_HOSTS
          valueFrom:
            configMapKeyRef:
              name: graylog-config
              key: OPENSEARCH_HOSTS
        - name: DISABLE_SECURITY_DASHBOARDS_PLUGIN
          valueFrom:
            configMapKeyRef:
              name: graylog-config
              key: DISABLE_SECURITY_DASHBOARDS_PLUGIN

---
apiVersion: v1
kind: Service
metadata:
  name: opensearch-dashboards
  namespace: graylog
spec:
  selector:
    app: opensearch-dashboards
  ports:
  - port: 5601
    targetPort: 5601
  type: ClusterIP
